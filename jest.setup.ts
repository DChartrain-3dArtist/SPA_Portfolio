// Importe des "matchers" supplémentaires pour Jest depuis @testing-library/jest-dom.
// Cela nous permet d'utiliser des assertions plus lisibles et spécifiques au DOM,
// comme .toBeInTheDocument(), .toHaveTextContent(), etc.
import '@testing-library/jest-dom';
